/** @deprecated Legacy app_content slug — use `GET /api/delivery-threshold` instead. */
export const DELIVERY_THRESHOLD_CONTENT_SLUG = 'delivery_threshold';

/** Fallback when CMS is missing or unparsable. */
export const DEFAULT_FREE_DELIVERY_THRESHOLD_GBP = 100;
