/** Promotion strip — `GET /api/app-promotion-banner` (Shopify type `app_promotion_banner`). */
export const APP_PROMOTION_BANNER_API_PATH = '/api/app-promotion-banner';

/** Fixed strip height under the header row when visible (px). */
export const APP_PROMOTION_BANNER_STRIP_HEIGHT = 52;
